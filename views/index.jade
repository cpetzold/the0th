#c
  #t0.e: span 0
  .e 
  .e 
  .e 
  .e 
  .e 
  .e 
  .e 
  .e 
  .e 
  .e 
  .e 
  .e 
  .e 
  .e 
  .e 


script
  s = io.connect('http://localhost');
  
  $(function() {
    var $e = $('.e')
      , $w = $(document)
      , i = 5
      , ww = $w.width() / 2
      , wh = $w.height() / 2
      , dark = '#101010'
      , light = '#ccc';
    
    $(document).mousemove(function(e) {
      var x = e.clientX
        , y = e.clientY;
      
      $e.each(function() {
        var $el = $(this)
          , o = $el.offset()
          , w = 110
          , h = $el.innerHeight()
          , dx = o.left + (w / 2) - x
          , dy = o.top + (h / 2) - y;
          
        var distance = i * Math.sqrt((dx*dx)+(dy*dy)) / wh
          , shadow = '0px ' + distance + 'px ';

        shadow += ($el.attr('id') == 't0') ? light : dark;
        
        $el.css({
          boxShadow: shadow,
          '-webkit-box-shadow': shadow
        });
      });
    });
  }); 

